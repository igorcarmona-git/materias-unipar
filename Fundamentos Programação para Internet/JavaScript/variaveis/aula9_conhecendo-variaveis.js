/* 3 palavras reservadas para se criar uma variável:
    - var
    - let
    - const
*/

var clima = "Quente"
clima = "Frio"

let clima2 = "Nublado"

const clima3 = "Escuro" // Constante não muda o valor, se tentar mudar, acusa erro.
console.log(clima, clima2)