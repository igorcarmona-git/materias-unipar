// Podemos declarar funções dentro de variáveis

// function expression or function anonymous
const sum = function(number1, number2){
                    //function parameters
                    // number1, number2 só funcionam na função, depois que a função acaba, não funciona mais.
    console.log(number1 + number2)
    // função sem retorno, executa e acaba.
}

sum(2, 3) //arguments
